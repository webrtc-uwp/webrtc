#include "config.h"

//TODO: this should return expected because parsing can fail or the config can be
//full of shit
Config Config::from_file(absl::string_view file_path) {
  // TODO: something meaningful
  return Config{};
}